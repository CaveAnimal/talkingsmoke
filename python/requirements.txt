torch==2.8.0
onnx==1.19.0
onnxruntime==1.23.0
numpy==2.2.6
pytest==8.4.2
# Note: These pins were tested with Python 3.10 on the development machine.
# Recommended: use Python 3.10 for CI and developer environments for the broadest
# compatibility with PyTorch, ONNX and ONNX Runtime. Python 3.9 and 3.11 are
# also generally supported; avoid 3.12+ until binary wheels are confirmed for
# your target platform.
# Note: These are recommended dependencies for full model training and export.
# On lightweight environments you can skip installing heavy packages until needed.